#include "main.h"

/**
 * print_numbers - prints most numbers
 *
 * Retrun: void
 */

void print_most_numbers(void)
{
char i;

for (i = '0'; i <= '9'; i++)
{
	if (!(c == '2' || c == '4'))
		_putchar(i);
}
	_putchar('\n');
}
